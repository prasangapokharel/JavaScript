//Function variable is used to sort numerical data

const num = [20, 50, 233, 55, 12];
num.sort(function(a,b) {return a-b}) //If a-b value = negative result is a, | //If a-b value = positive result is b,
console.log(num)